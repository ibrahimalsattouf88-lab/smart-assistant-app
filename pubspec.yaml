name: smart_assistant_app
description: Smart Assistant with VA button and ASR.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

  # الشبكة والبيئة
  http: ^1.2.2
  flutter_dotenv: ^5.1.0

  # الأذونات/المايك
  permission_handler: ^12.0.1

  # التعرف الصوتي
  speech_to_text: ^7.3.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
