workflows:
  android-build:
    name: Android Build (Smart Assistant)
    environment:
      flutter: stable
      xcode: latest
      cocoapods: 1.16.2 # Updated to force Codemagic sync
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Install specific CocoaPods version
        script: |
          gem install cocoapods -v 1.16.2
      - name: Set up Flutter
        script: | 
          flutter clean
          flutter pub get
      - name: Build Android
        script: | 
          flutter build apk --release
    artifacts:
      - build/app/outputs/**/*.apk
    publishing:
      email:
        recipients:
          - ibrahimalsattouf88@gmail.com
        notify:
          success: true
          failure: true
